Sources for the plugin `plugin.appodeal.A4G`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.A4G" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
